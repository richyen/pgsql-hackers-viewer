services:
  # Backend API
  - type: web
    name: pgsql-analyzer-backend
    runtime: docker
    dockerfilePath: ./Dockerfile.backend
    dockerContext: .
    env: docker
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pgsql-db
          property: connectionString
      - key: PORT
        value: 8080
      - key: API_PORT
        value: 8080
      - key: API_HOST
        value: 0.0.0.0
      - key: ENV
        value: production
      - key: DATA_DIR
        value: /tmp/data
      - key: ARCHIVE_USERNAME
        value: archives
      - key: ARCHIVE_PASSWORD
        value: antispam
      - key: MAILING_LIST_EMAIL
        value: pgsql-hackers@postgresql.org

  # Frontend
  - type: web
    name: pgsql-analyzer-frontend
    runtime: docker
    dockerfilePath: ./Dockerfile.frontend
    dockerContext: .
    env: docker
    plan: free
    region: oregon
    envVars:
      - key: REACT_APP_API_URL
        value: https://pgsql-analyzer-backend.onrender.com

databases:
  - name: pgsql-db
    databaseName: pgsql_analyzer
    plan: free
    region: oregon
